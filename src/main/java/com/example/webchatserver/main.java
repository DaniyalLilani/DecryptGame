package com.example.webchatserver;

import jakarta.websocket.Session;
import java.io.IOException;

public class main {
    public static void main(String[] args) {
        /*
        // Create instances of necessary classes
        SharedContext sharedContext = SharedContext.getInstance();
        ChatServer webSocketServer = new ChatServer();

        // Sample JSON message for updating names
        String updateNamesJson = "{\"type\":\"update-names\",\"player1\":\"Alice\",\"player2\":\"Bob\"}";

        // Creating a dummy session for demonstration
        Session dummySession = null; // You need to initialize a Session object here

        try {
            // Calling the handleMessage method on the WebSocketServer instance
            webSocketServer.handleMessagetest(updateNamesJson, dummySession);
        } catch (IOException e) {
            e.printStackTrace();
        }
    }*/
}}
